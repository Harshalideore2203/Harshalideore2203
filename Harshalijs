Here is the revised JavaScript code snippet that uses static classes to target the ETA element:

var portName = "ABIDJAN - ABIDJAN TERMINAL";
var AllTableElements = document.querySelectorAll(".ant-table-row");

AllTableElements.forEach((element, index) => {
var portElement = element.querySelector(".mobileDivView .ant-row:first-child .rowLabel");
if (portElement) {
if (portElement.innerText.trim() === portName.trim()) {
var ETAElement = element.querySelector(".mobileDivView .ant-row .headerLabel:contains(Arrival Date) + .rowLabel");
if (ETAElement) {
console.log("ETA element found");
var ETA = ETAElement.innerText;
console.log("ETA:", ETA);
}
}
}
});
