It seems that the code is not able to find the ETA element. Here's a modified version of the code that adds some error checking:

```
// Define variables
var portName = "ABIDJAN";
var AllTableElements = document.querySelectorAll("tbody tr");

// Loop through table elements
AllTableElements.forEach((element, index) => {
  var portElement = element.querySelector("div.mobileDivView > div.ant-row:nth-child(1) > div > label.rowLabel");
  if (portElement) {
    console.log("Port element found");
    if (portElement.innerText === portName) {
      console.log("Port name matches");
      var ETACheckDateElement = element.querySelector("div.mobileDivView > div.ant-row:nth-child(6) > div > label.rowLabel");
      if (ETACheckDateElement) {
        console.log("ETA element found");
        var ETACheckDate = ETACheckDateElement.innerText;
        console.log("ETA:", ETACheckDate);
      } else {
        console.log("ETA element not found");
      }
    } else {
      console.log("Port name does not match");
    }
  } else {
    console.log("Port element not found");
  }
});
```

This code will log messages to the console indicating whether the port element, port name, and ETA element are found. This should help you diagnose the issue.
