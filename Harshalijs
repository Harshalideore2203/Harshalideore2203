Yes, you can use `document.getElementsByClassName` to target the `.ant-table-tbody` element and then access its child nodes.

Here's an example:

```
var tbody = document.getElementsByClassName("ant-table-tbody")[0];
var rows = tbody.childNodes;

for (var i = 0; i < rows.length; i++) {
  var row = rows[i];
  var ETAElement = row.querySelector(".headerLabel:contains(Arrival Date) + .rowLabel");
  
  if (ETAElement) {
    console.log("ETA element found");
    var ETA = ETAElement.innerText;
    console.log("ETA:", ETA);
  }
}
```

This code targets the first `.ant-table-tbody` element, accesses its child nodes (which are the table rows), and then loops through each row to extract the ETA element.
