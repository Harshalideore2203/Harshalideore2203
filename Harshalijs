Based on the provided HTML content, I found that the static classes for the ETA element are:

.ant-table-row
.mobileDivView
.ant-row
.rowLabel

Here's an updated JavaScript code snippet to extract the ETA:

```
var portName = "ABIDJAN - ABIDJAN TERMINAL";
var AllTableElements = document.querySelectorAll(".ant-table-row");

AllTableElements.forEach((element, index) => {
  var portElement = element.querySelector(".mobileDivView .ant-row .rowLabel");
  if (portElement) {
    if (portElement.innerText.trim() === portName.trim()) {
      var ETACheckDateElement = element.querySelectorAll(".mobileDivView .ant-row")[7].querySelector(".rowLabel");
      if (ETACheckDateElement) {
        console.log("ETA element found");
        var ETACheckDate = ETACheckDateElement.innerText;
        console.log("ETA:", ETACheckDate);
      }
    }
  }
});
```
