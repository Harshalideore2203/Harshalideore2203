Here's the modified code that takes the vessel name as a variable and loops through the table rows to find the ETA, ETD, and LVR (Voyage) for that vessel:

```
// Initialize variables
let vesselName = "LUCIEN G A"; // Replace with your vessel name
let eta = "2024-11-28"; // Replace with your ETA date
let FetchedETA = '';
let FetchedETD = '';
let ImportLVR = '';
let OutputVar = '';

// Calculate date range
let ETADate = new Date(eta);
let ETAvalplus1 = new Date(ETADate.getTime() + 86400000); // +1 day
let ETAvalplus2 = new Date(ETADate.getTime() + 172800000); // +2 days
let ETAvalplus3 = new Date(ETADate.getTime() + 259200000); // +3 days
let ETAvalplus4 = new Date(ETADate.getTime() + 345600000); // +4 days
let ETAvalplus5 = new Date(ETADate.getTime() + 432000000); // +5 days

let ETAvalminus1 = new Date(ETADate.getTime() - 86400000); // -1 day
let ETAvalminus2 = new Date(ETADate.getTime() - 172800000); // -2 days
let ETAvalminus3 = new Date(ETADate.getTime() - 259200000); // -3 days
let ETAvalminus4 = new Date(ETADate.getTime() - 345600000); // -4 days
let ETAvalminus5 = new Date(ETADate.getTime() - 432000000); // -5 days

// Extract table rows
const tableRows = document.getElementsByClassName('ant-table-row');

// Loop through table rows
for (let i = 0; i < tableRows.length; i++) {
  const row = tableRows[i];
  const vesselElement = row.querySelector('.generalLinkLabel');
  
  if (vesselElement && vesselElement.textContent.trim() === vesselName) {
    // Extract ETA date
    const etaElement = row.querySelectorAll('.rowLabel')[1];
    FetchedETA = etaElement.textContent.trim();
    
    // Check if ETA date matches the calculated date range
    if (FetchedETA === eta ||
      FetchedETA === ETAvalplus1.toISOString().split('T')[0] ||
      FetchedETA === ETAvalplus2.toISOString().split('T')[0] ||
      FetchedETA === ETAvalplus3.toISOString().split('T')[0] ||
      FetchedETA === ETAvalplus4.toISOString().split('T')[0] ||
      FetchedETA === ETAvalplus5.toISOString().split('T')[0] ||
      FetchedETA === ETAvalminus1.toISOString().split('T')[0] ||
      FetchedETA === ETAvalminus2.toISOString().split('T')[0] ||
      FetchedETA === ETAvalminus3.toISOString().split('T')[0] ||
      FetchedETA === ETAvalminus4.toISOString().split('T')[0] ||
      FetchedETA === ETAvalminus5.toISOString().split('T')[0]) {
      
      // Extract ETD date
      const etdElement = row.querySelectorAll('.rowLabel')[2];
      FetchedETD = etdElement.textContent.trim();
      
      // Extract LVR (Voyage)
      const lvrElement = row.querySelectorAll('.rowLabel')[3];
      ImportLVR = lvrElement.textContent.trim();
      
      // Construct output string
      OutputVar = `Vessel: ${vesselName}, ETA: ${FetchedETA}, ETD: ${FetchedETD}, LVR: ${ImportLVR}`;
      
      console.log(OutputVar);
    }
  }
}
```

This code loops through the table rows, checks if the vessel name matches the target vessel name, and then extracts the ETA, ETD, and LVR (Voyage) for that vessel. It also checks if the ETA date matches the calculated date range.
